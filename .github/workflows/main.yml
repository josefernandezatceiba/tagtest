name: reusable workflows getting started
on:
  push:
    branches: main

jobs:
  base:
    uses: josefernandezatceiba/tagest/.github/workflows/setup_build.yml@main
    
  additional-steps:
    needs: base
    runs-on: ubuntu-latest
    steps: 
      - name: zipping stuff
        working-directory: ./
        run: |
          pwd && ls -li
      
    
      
          
    
  
    
   
    
    
          
