name: reusable workflows getting started
on:
  push:
    branches: main

jobs:
  base:
    uses: ./.github/workflows/setup_build.yml
    
  additional-steps:
    needs: base
    runs-on: ubuntu-latest
    steps: 
      - name: zipping stuff
        working-directory: ./
        run: |
          pwd && ls -li
      
    
      
          
    
  
    
   
    
    
          
